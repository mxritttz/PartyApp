export type EventData = {
    title: string;
    description: string;
    host: string;
    date: Date;
    location: string;
    metadata: any;
}

export type Event = {
    id: string;
    title: string;
    description: string;
    host: string;
    date: Date;
    location: string;
    metadata: any;
}